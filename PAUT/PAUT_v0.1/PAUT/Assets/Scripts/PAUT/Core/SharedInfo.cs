using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace PAUT
{
    public class SharedInfo : MonoBehaviour
    {
        public static TriggerInput curInput;
        // here we assume that the input should be identical during training

    // Use this for initialization
    void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}


